properties:
  test: fromconfigserver222
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  tracing:
    sampling:
      probability: 1.0
  endpoints:
    web:
      exposure:
        include: *


spring:
  zipkin:
    baseUrl: http://127.0.0.1/:9411/     
